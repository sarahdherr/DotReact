// import * as React from "react";
// import { shallow } from "enzyme";

// import { Score } from "../../components/Score";

// it("score prop is displayed", () => {
//     const result = shallow(<Score score={88} />).contains(<p className="listitem-score-text">{88}</p>);
//     expect(result).toBeTruthy();
// });

// it("adds `0` in front of single digit numbers", () => {
//     const result = shallow(<Score score={8} />).contains(<p className="listitem-score-text">0{8}</p>);
//     expect(result).toBeTruthy();
// })